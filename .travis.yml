language: generic 
sudo: required
services: 
  - docker

before_install:
  - docker build -t buzzibox/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true buzzibox/docker-react npm run test